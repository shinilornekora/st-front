# URL бэкенд-сервера для Vite dev proxy (только для локальной разработки)
# В Docker Compose укажи имя сервиса, например: http://backend:3000
# По умолчанию: http://localhost:3000
VITE_API_TARGET=http://localhost:3000
